version: 2.1

jobs:
  build-lint-and-test:
    machine:
      image: ubuntu-2004:current

    steps:
      - checkout
      - run: make deps
      - run: make lint
      - run: make test
workflows:
  basic:
    jobs:
      - build-lint-and-test
